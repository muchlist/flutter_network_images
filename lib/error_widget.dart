import 'package:flutter/material.dart';

class ErrorxWidget extends StatelessWidget {
  const ErrorxWidget();

  @override
  Widget build(BuildContext context) {
    return const Icon(
      Icons.error,
      size: 25,
      color: Colors.red,
    );
  }
}
